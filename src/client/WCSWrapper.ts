

// WCS
import { ReplicatedStorage } from "@rbxts/services";
import { CreateClient } from "@rbxts/wcs";
import { Character } from "@rbxts/wcs";

// WCS Shared Folder
const ParentFolder = ReplicatedStorage.FindFirstChild("WCS", true);

export class WCSWrapper {
    constructor() {
        // WCS Client Startup
    }
}