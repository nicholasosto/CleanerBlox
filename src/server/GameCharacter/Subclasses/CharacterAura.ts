import { GameStorage } from "shared/Utility/GameStorage";
import { InsertService } from "@rbxts/services";

/*
export class CharacterAura {
    private _auraFrame: Frame;

    constructor(character: Model, name: string) {
        this._auraFrame = auraFrame;
        this._auraFrame.SetAttribute("TextValue", name);
    }

    public setAuraActive(active: boolean) {
        this._auraFrame.SetAttribute("AuraActive", active);
    }
}
    */